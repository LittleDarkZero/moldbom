# 遗留问题清单

> 排查日期：2026-08-13 ｜ 排查方式：全量代码通读 + 三套测试复跑 + 用户确认基准回归复跑
> 状态标记：⬜ 未解决 ｜ ✅ 已闭环
> 修复顺序建议：P0 → P1 → P2，每项修复后更新状态并在「验证记录」注明验证方式与结果

---

## P0 — 数据正确性（最高优先，直接影响 BOM 输出可信度）

### P0-1 规格测量引擎与用户确认基准存在真实回归（0/50）
- **问题描述**（2026-08-13 全面体检，逐件诊断 50 点云）：
  1. **形状误判（最严重，31 件）**：5 组真实圆柱件被判为 box——定1地侧支撑柱×5(Φ80×150)、定1定位圈(Φ250×40)、动模定位圈(Φ200×40)、套筒顶杆×20(Φ15)、地侧支撑柱×4(Φ100×130)，判定链均命中 `proj_3rect`。**根因**：真实点云仅 16~100 个 STP 边界折点（合成测试用 3000 点），08-11 投影特征分类的"圆投影"判据在稀疏多边形顶点上失效，且高置信（0.92）提前返回跳过了老链路。
  2. **圆柱尺寸精度问题**：挡料环 Φ110 实测 Φ104.4（差 5.6mm，投影尺寸替代了最小体积圆柱）；导柱 70→69.5（0.5mm，取整可消）。
  3. **格式漂移（次要）**：08-11 `format_spec` 保留一位小数，基准比较侧未归一化。
- **修复方案**（2026-08-13 已实施）：① 新增 `PROJ_SPARSE_N=200` 门控——稀疏点云投影分类置信度封顶在 PROJ_CONF_FLOOR 之下不提前返回，交回 PCA+圆形度启发式（08-03 用户确认链路）判定形状，直径/高度由 33 方向最小体积圆柱计算（老引擎 Φ110/Φ70 的正确来源）；② verify_user_confirmed.py 比较侧归一化（×→*、.0 去尾）+ 数值四舍五入取整对齐整数基准，前缀型期望（Φ15×）按前缀匹配。
- **验证**：50 点云基准 **50/50 通过**；test_bom_logic 18/18（合成点云行为不变，斜置 box/正方体/圆柱/圆盘回归保护）；test_stp_features 17/17。
- **涉及模块**：`geometry_engine.py`（_classify_by_projection / PROJ_SPARSE_N）、`verify_user_confirmed.py`
- **状态**：✅ 已闭环（2026-08-13，50/50）

### P0-2 双规则系统并存且不同步
- **问题描述**：~~`gr_rules.json` 的 `learned_mapping`=0 条、`companion_rules.rules`=0 条，生产规则全部在 V2~~（2026-08-13 已整体删除老系统）
- **涉及模块**：`bom_export.py`、`v2_bridge.py`、`V2/rules/`
- **处置**：**老规则系统整体删除**（gr_rules.json / rule_data / rule_gui / rule_advanced / rule_editor / xls_to_rule_data 已隔离至 `%TEMP%/TOOL_cleanup_20260813/老规则系统/`）；bom_export 全部规则推理改走 V2 引擎（唯一规则源）；`--learn` 学习模式删除。结构配置迁移为 V2 规则 23 条（filter 2 / normalize 14 / number 3 / measure 1 / purchase 1 / companion 2，快照 v2.0.19）；merge 名单与模号截断段为 V2 无对应域的结构常量。**gr 域业务规则未录全的部分见 P1-7（用户后续手动补全）**。
- **状态**：✅ 已闭环（2026-08-13）

### P0-3 双测量引擎未收敛
- **问题描述**：~~stp_features（面参数 B-rep）与 geometry_engine（点云）职责重叠~~（2026-08-13 已收敛）
- **处置（2026-08-13 收敛决策）**：**规格测量唯一权威 = geometry_engine**（50 点云用户确认基准 50/50 闭环验证）；stp_features 职责收敛为**面级特征提取工具**（孔提取 extract_holes + 孔径→螺钉匹配 match_screw，供 P1-8 CB 校验等消费），其 analyze_stp 包围盒/规格输出仅供诊断交叉验证、不进入 BOM 主链（代码注释+README 已明确职责边界）。
- **涉及模块**：`stp_features.py`、`geometry_engine.py`、`README.md`
- **状态**：✅ 已闭环（2026-08-13 职责收敛）

### P0-4 异常路径资源泄漏
- **问题描述**：~~pipeline 任一阶段抛异常时，src_doc.Close() 与 shutil.rmtree(temp_dir) 不会执行（在 try 块之外）~~（2026-08-13 已修复）
- **修复方案（2026-08-13）**：① `process_one_part` 关闭文档移入 finally（pipeline 异常也关）；② `_batch_parse_one` 文档关闭+临时目录清理全部 finally 化；③ `main()` 单文件模式改走 process_one_part + finally 清临时目录；④ `_restore_catia_session` 补 DisplayFileLockAlerts（与 _setup 全对称），GUI finally 改调该函数。
- **已知边界（保留说明）**：进程被硬杀（任务管理器）时 Python 代码无法执行任何清理，CATIA 停留 RefreshDisplay=OFF/弹窗抑制状态属平台限制，重开程序后 _setup 会重设状态（关刷新幂等），仅界面不刷新需手动 F5。
- **涉及模块**：`bom_export.py`（process_one_part / _batch_parse_one / main / _restore_catia_session）、`bom_gui.py`（_run finally）
- **状态**：✅ 已闭环（2026-08-13，异常路径 finally 化）

---

## P1 — 健壮性与一致性（中优先）

### P1-5 字符串契约脆弱（配套件"→"前缀）
- **问题描述**：配套件识别靠 `备注 = "→ 父件名"` 前缀约定，编号、拆分、排序三处依赖。合并规则已删（2026-08-13），原"合并件名前缀双向匹配"部分随之清除；但"→"前缀约定本身仍在。
- **涉及模块**：`bom_export.py`（add_companions / assign_part_numbers / export_split_parts）
- **修复方向**：引入结构化字段（`_is_companion` / `_parent_ref`）替代字符串约定。
- **状态**：✅ 已闭环（2026-08-19）——`add_companions` 产出 `_is_companion`/`_parent_ref`（并透传 `_source`）；`assign_part_numbers`/`export_split_parts` 优先读结构化字段，旧 "→ " 前缀仅作兼容回退；批量模式配套件按 `_source` 配对防跨文件串号。新增回归用例 `test_companions_structured_fields` / `test_assign_numbers_batch_source_matching`。

### P1-6 rule_editor.py 无鉴权 + 与 rule_gui 冗余
- **问题描述**：~~旧 web 编辑器（8765 端口）任意 POST /api/save 即可覆盖 gr_rules.json~~（2026-08-13 随老规则系统整体删除）
- **涉及模块**：~~`rule_editor.py`、`rule_editor.html`~~
- **状态**：✅ 已闭环（2026-08-13 删除）

### P1-7 V2 规则业务规则未录全（过渡期 GR 回落）
- **问题描述**：2026-08-13 结构性配置已迁入 V2（filter/normalize/number/measure/purchase/companion 共 23 条），gr/material/remark 域业务规则由用户持续手动补全中。当前 filter 域已合并为一条（毛坯|工艺柱|减槽|活塞|STAUBLI|接头）。**剩余缺口**（模架类核心关键词等）由用户掌握录入进度；未命中的零件当前回落「小零件」。**关键词冲突靠显式 priority 手动调整**（默认 500，editor.py/table_editor.py 均已支持）。
- **涉及模块**：`V2/rules/*.rules.json`、`v2_bridge.py`、`V2/tests/test_engine.py`（12 用例因规则未录跳过）
- **复现方式**：`python -m rulespec infer "定1模框" --no-name-spec` → GR=小零件（应=模架）；补全规则后应命中 gr 域规则。
- **状态**：⬜ 未解决（等用户补全规则）

### P1-8 CB>M20 / CBW>16 规格校验未实现
- **问题描述**：业务决策要求螺钉规格上限校验（CB>M20、CBW>16 拦截），代码未实现；stp_features.match_screw 对照表到 M24 且无上限策略。
- **涉及模块**：`stp_features.py`（match_screw）、配套校验点（bom_export 目前无此校验）
- **复现方式**：构造 CB24 配套件导出，无任何告警；Φ17 通孔匹配无上限拦截。
- **修复方向**：在配套生成处增加校验拦截 + 日志告警。
- **状态**：✅ 已闭环（2026-08-19）——`bom_companions.add_companions` 增加 `_companion_over_limit` 校验（CB>M20、CBW>16 拦截，不进入 BOM 并 `log.warning` 告警）；新增回归用例 `test_companion_over_limit_blocked`。注：`stp_features.match_screw` 仅作孔→螺钉诊断匹配，不属于 BOM 配套生成链，未改其对照表。

### P1-9 非建模件缺失（BOM 缺行）
- **问题描述**：销钉/隔水片/吊环/O型圈/水管/铭牌等在 CATPart 中无 Body，BOM 无法生成对应行；模架螺钉数量待建模完善；零件名统一（定1 vs 定模1）待用户规范。
- **涉及模块**：`bom_export.py` parse 阶段、用户建模流程
- **复现方式**：明细表含销钉的模具跑导出，与明细表对比 BOM 缺行。
- **修复方向**：建模补齐 / 或明细表兜底合并方案（需用户决策）。
- **状态**：⬜ 未解决

---

## P2 — 工程卫生（低优先）

### P2-10 文档与代码口径过时
- **问题描述**：SOUL.md 写"测试 13/13"实际 18 用例（2026-08-13 V2 化重写后）；bom_export.py 头注释已升 v9.2，GUI 标题仍为 v2；~~规则编辑器使用说明~~（已随老规则系统删除）。
- **涉及模块**：`SOUL.md`、`bom_gui.py`、`bom_export.py` docstring
- **复现方式**：`python test_bom_logic.py` → "18 passed"。
- **状态**：✅ 已闭环（2026-08-19）——README 测试数量已同步为 bom_logic 24 / stp_features 20 / engine 26 / namespec 7 / keyword 3；`V2/README.md` 9 域改 10 域、流水线补 `spec` 域；`manifest.json` 补 `spec` 域；`rulespec.__version__` 改为动态读取 manifest。

### P2-11 合并匹配与学习归一化不一致
- **问题描述**：~~merge_similar_parts 仅吃 merge_rules 精确名匹配；_normalize_part_name 只用于 learned 查找不用于合并~~（2026-08-13 合并规则整体删除，此问题随之消失）
- **状态**：✅ 已闭环（合并功能删除）

### P2-12 打包快照冗余
- **问题描述**：TOOL 根 `bom_export.zip` 312MB，内含 dist/build_new/__pycache__/TEST 全部内容，仅作快照用途。
- **涉及模块**：根目录快照管理
- **复现方式**：解压查看体积分布（dist 约 70MB、TEST 约 285MB）。
- **状态**：⬜ 未解决

---

## 后续展望（非缺陷，暂缓实现）

### FUT-1 拆分后自动发邮件给外协（蔡师傅）
- **需求**：`--split` 拆分完成后，把「发给蔡师傅」文件夹下的明细表自动通过邮件发给 `binc01@hascovision.com`。
- **现状与阻塞点**（2026-08-13 分析）：① 企业微信（wecom）连接器**不支持发邮件**（只有消息/文档/日程/会议/待办）；② 程序是独立 exe，无法调用对话内的「智能体邮箱」（agent-mail）。
- **可选路线**：A) 程序内嵌 SMTP 自动发（需提供发件邮箱 + SMTP 服务器 + 端口 + 授权码，可外置配置文件避免明文）；B) 对话内用 agent-mail 手动/半自动发（不打包进程序）。
- **状态**：⏸ 待用户提供 SMTP 配置或选择路线后实施。

---

## 验证记录

| 日期 | 项目 | 结果 |
|---|---|---|
| 2026-08-13 | 清理测试脚本/临时数据（11 项移入隔离目录） | ✅ 项目残留扫描干净 |
| 2026-08-13 | 恢复 `TEST/规格测量基准_用户确认.json`（自 bom_export.zip） | ✅ 已恢复（1335B，12 组期望） |
| 2026-08-13 | `test_bom_logic.py` | ✅ 18/18 通过（V2 化重写后） |
| 2026-08-13 | `V2/tests/test_engine.py` | ⚠️ 13 通过 / 12 跳过（规则未录完） |
| 2026-08-13 | `test_stp_features.py` | ✅ 17/17 通过 |
| 2026-08-13 | `verify_user_confirmed.py`（50 点云基准） | ❌ 0/50 失败 → 见 P0-1（修复前） |
| 2026-08-13 | 老规则系统删除（P0-2/P1-6 闭环） | ✅ 23 条结构规则迁入 V2，快照 v2.0.19，门禁通过 |
| 2026-08-13 | V2 真实链路冒烟（定1模框/热流道/毛坯件） | ✅ filter 过滤生效；GR 回落小零件（待补规则） |
| 2026-08-13 | **P0-1 修复**：投影分类稀疏点云门控（PROJ_SPARSE_N=200）+ 基准比较归一化 | ✅ **50/50 通过**；18/18 单测不破；17/17 stp 不破 |
| 2026-08-13 | **P0-4 修复**：process_one_part/_batch_parse_one/main 异常路径 finally 化 + 会话恢复对称 | ✅ 18/18 + 50/50 + 17/17 全绿 |
| 2026-08-13 | **P0-3 收敛**：geometry_engine=唯一测量权威，stp_features=面级特征工具（诊断用） | ✅ 职责边界写入代码注释+README |
| 2026-08-13 | **薄板误判修复**：gap_ratio（最小包围圆半径/截面短半宽）拦细长矩形截面，圆柱1~4/薄板≥6.7 | ✅ 真实 STP 调整板/压线板→box、导柱/支撑柱→cylinder；50/50+22/22+17/17 |
| 2026-08-13 | **退化点云兜底**：复位杆调整板圆盘 STP 只导出共面顶点 → stp_features 面级兜底取 Φ31.5×12 | ✅ 50/50 + 22/22 + 17/17 |
| 2026-08-13 | **带孔方板误判修复**：stp_features._cyl_is_hole 改用边界圆法向可靠轴 + CYL_HOLE_RATIO 1.5 + _measure_one_spec 交叉验证 | ✅ 013→120×120×12、005/006 全对，导柱/定位圈不变；50/50 |
| 2026-08-13 | **规格格式回退**：保留 1 位小数、整数去 .0（79.5→79.5、40.0→40） | ✅ 压线板 1.9/49.8 保留；50/50 + 22/22 + 17/17 |
| 2026-08-13 | **删除合并规则 + filter 加水管**：删 MERGE_PATTERNS/merge_similar_parts/_stage_merge（7→6 阶段）；filter 加"水管"后用户合并为一条（毛坯\|工艺柱\|减槽\|活塞\|STAUBLI\|接头） | ✅ 21/21 + 50/50 + 17/17 + 13/0；门禁 207 条 v2.0.33 |
| 2026-08-13 | **优先级手动 + 删长度加权 + 紧固件按GR拆分**：删引擎关键词长度加权（同长度失效）；table_editor 补优先级输入框；_group_by_gr 配套件按各自 GR 归组 | ✅ 21/21 + 50/50 + 17/17 + 13/0 |
| 2026-08-13 | **直径符号归一化**：canonical_spec 加 ⌀/ϕ/⍉→Φ（规则录 ⌀ 测量出 Φ 导致圆柱件规格级规则全失配）；outputSpec ⌀32→D32 | ✅ 复位杆调整板 Φ31.5×12→D32 命中；13/0 + 21/21 |
| 2026-08-13 | **紧固件数量修正**：规则录 qty=单件数量，总数量=单件×父件总数（压线板D 9×2=18） | ✅ 21/21 |
| 2026-08-13 | **加工备注去"含X个实体"**：_measure_one_spec 不再生成实体数备注，统一走 V2 remark 域 | ✅ 21/21 |
| 2026-08-13 | **拆分按 GR 分文件夹 + 每 GR 单独 zip**：CATPart ASCII 存临时再 move 到 {GR}/{零件号}-{零件名称}/；每 GR 单独 make_archive | ✅ 模拟 zip 结构正确；21/21 |
| 2026-08-13 | **拆分加"发给蔡师傅"文件夹**：汇总完整总 BOM + 细分明细表 | ✅ 模拟验证通过；21/21 |
